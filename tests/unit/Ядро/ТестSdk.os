// BSLLS:MagicNumber-off
// BSLLS:MissingVariablesDescription-off
// BSLLS:PublicMethodsDescription-off
// BSLLS:MissingParameterDescription-off
// BSLLS:MissingReturnedValueDescription-off
// BSLLS:DuplicateStringLiteral-off
#Использовать "../../.."
#Использовать asserts
#Использовать moskito

&ПослеКаждого
Процедура Очистка() Экспорт
    ОтелГлобальный.Сбросить();
КонецПроцедуры

&Тест
Процедура ПостроениеSdk() Экспорт

    // Дано
    МокПровТрасс = Новый Структура("Тип", "Трассировка");
    МокПровЛог = Новый Структура("Тип", "Логирование");
    МокПровМетр = Новый Структура("Тип", "Метрики");

    // Когда
    Сдк = Новый ОтелSdk(МокПровТрасс, МокПровЛог, МокПровМетр);

    // Тогда
    Ожидаем.Что(Сдк.ПровайдерТрассировки()).Равно(МокПровТрасс);
    Ожидаем.Что(Сдк.ПровайдерЛогирования()).Равно(МокПровЛог);
    Ожидаем.Что(Сдк.ПровайдерМетрик()).Равно(МокПровМетр);

КонецПроцедуры

&Тест
Процедура ПостроительFluent() Экспорт

    // Дано
    МокПровТрасс = Новый Структура("Тип", "Трассировка");
    МокПровЛог = Новый Структура("Тип", "Логирование");
    МокПровМетр = Новый Структура("Тип", "Метрики");

    // Когда
    Сдк = Новый ОтелПостроительSdk()
        .УстановитьПровайдерТрассировки(МокПровТрасс)
        .УстановитьПровайдерЛогирования(МокПровЛог)
        .УстановитьПровайдерМетрик(МокПровМетр)
        .Построить();

    // Тогда
    Ожидаем.Что(Сдк.ПровайдерТрассировки()).Равно(МокПровТрасс);
    Ожидаем.Что(Сдк.ПровайдерЛогирования()).Равно(МокПровЛог);
    Ожидаем.Что(Сдк.ПровайдерМетрик()).Равно(МокПровМетр);

КонецПроцедуры

&Тест
Процедура ПостроитьИЗарегистрироватьГлобально() Экспорт

    // Дано
    МокПров = Новый Структура("Тип", "Трассировка");

    // Когда
    Сдк = Новый ОтелПостроительSdk()
        .УстановитьПровайдерТрассировки(МокПров)
        .ПостроитьИЗарегистрироватьГлобально();

    // Тогда
    Ожидаем.Что(ОтелГлобальный.Получить()).Равно(Сдк);

КонецПроцедуры

&Тест
Процедура СброситьБуфер() Экспорт

    // Дано
    ТекстПровайдера = "
    |Перем ВызванСброситьБуфер Экспорт;
    |Функция СброситьБуфер(ТаймаутМс = 0) Экспорт // BSLLS:UnusedParameters-off
    |    ВызванСброситьБуфер = Истина;
    |    Возврат ОтелРезультатыЭкспорта.Успех();
    |КонецФункции
    |Процедура Закрыть() Экспорт
    |КонецПроцедуры
    |ВызванСброситьБуфер = Ложь;";
    МокПров1 = ЗагрузитьСценарийИзСтроки(ТекстПровайдера);
    МокПров2 = ЗагрузитьСценарийИзСтроки(ТекстПровайдера);
    МокПров3 = ЗагрузитьСценарийИзСтроки(ТекстПровайдера);

    Сдк = Новый ОтелSdk(МокПров1, МокПров2, МокПров3);

    // Когда
    Сдк.СброситьБуфер();

    // Тогда
    Ожидаем.Что(МокПров1.ВызванСброситьБуфер).Равно(Истина);
    Ожидаем.Что(МокПров2.ВызванСброситьБуфер).Равно(Истина);
    Ожидаем.Что(МокПров3.ВызванСброситьБуфер).Равно(Истина);

КонецПроцедуры

&Тест
Процедура Закрыть() Экспорт

    // Дано
    ТекстПровайдера = "
    |Перем ВызванЗакрыть Экспорт;
    |Функция СброситьБуфер(ТаймаутМс = 0) Экспорт // BSLLS:UnusedParameters-off
    |    Возврат ОтелРезультатыЭкспорта.Успех();
    |КонецФункции
    |Функция Закрыть(ТаймаутМс = 0) Экспорт // BSLLS:UnusedParameters-off
    |    ВызванЗакрыть = Истина;
    |    Возврат ОтелРезультатыЗакрытия.Успех();
    |КонецФункции
    |ВызванЗакрыть = Ложь;";
    МокПров1 = ЗагрузитьСценарийИзСтроки(ТекстПровайдера);
    МокПров2 = ЗагрузитьСценарийИзСтроки(ТекстПровайдера);
    МокПров3 = ЗагрузитьСценарийИзСтроки(ТекстПровайдера);

    Сдк = Новый ОтелSdk(МокПров1, МокПров2, МокПров3);

    // Когда
    Результат = Сдк.Закрыть();

    // Тогда
    Ожидаем.Что(МокПров1.ВызванЗакрыть).Равно(Истина);
    Ожидаем.Что(МокПров2.ВызванЗакрыть).Равно(Истина);
    Ожидаем.Что(МокПров3.ВызванЗакрыть).Равно(Истина);
    Ожидаем.Что(Результат.Успех).Равно(Истина);

КонецПроцедуры

&Тест
Процедура SdkБезПровайдеров() Экспорт

    // Дано / Когда
    Сдк = Новый ОтелSdk();

    // Тогда
    Ожидаем.Что(Сдк.ПровайдерТрассировки()).Равно(Неопределено);
    Ожидаем.Что(Сдк.ПровайдерЛогирования()).Равно(Неопределено);
    Ожидаем.Что(Сдк.ПровайдерМетрик()).Равно(Неопределено);

    // СброситьБуфер и Закрыть не должны вызывать ошибку
    Сдк.СброситьБуфер();
    РезЗакрытия = Сдк.Закрыть();
    Ожидаем.Что(РезЗакрытия.Успех).Равно(Истина);

КонецПроцедуры

&Тест
Процедура ПолучитьТрассировщикShortcut() Экспорт

    // Дано
    МокПров = Мок.Получить(Тип("ОтелПровайдерТрассировки"));
    МокТрассировщик = Новый Структура("Имя", "тестовый");
    МокПров.Когда().ПолучитьТрассировщик(
        Матчеры.ЛюбоеЗначение(),
        Матчеры.ЛюбоеЗначение()
    ).ТогдаВозвращает(МокТрассировщик);
    Сдк = Новый ОтелSdk(МокПров);

    // Когда
    Результат = Сдк.ПолучитьТрассировщик("mylib", "1.0");

    // Тогда
    Ожидаем.Что(Результат).Равно(МокТрассировщик);
    МокПров.ПроверитьЧтоВызывалсяМетод().ПолучитьТрассировщик("mylib", "1.0");

КонецПроцедуры

&Тест
Процедура ПостроительТрассировщикаShortcut() Экспорт

    // Дано
    МокПров = Мок.Получить(Тип("ОтелПровайдерТрассировки"));
    МокПостроитель = Новый Структура("Имя", "построитель");
    МокПров.Когда().ПостроительТрассировщика(
        Матчеры.ЛюбоеЗначение()
    ).ТогдаВозвращает(МокПостроитель);
    Сдк = Новый ОтелSdk(МокПров);

    // Когда
    Результат = Сдк.ПостроительТрассировщика("mylib");

    // Тогда
    Ожидаем.Что(Результат).Равно(МокПостроитель);
    МокПров.ПроверитьЧтоВызывалсяМетод().ПостроительТрассировщика("mylib");

КонецПроцедуры

&Тест
Процедура ПолучитьЛоггерShortcut() Экспорт

    // Дано
    МокПров = Мок.Получить(Тип("ОтелПровайдерЛогирования"));
    МокЛоггер = Новый Структура("Имя", "логгер");
    МокПров.Когда().ПолучитьЛоггер(
        Матчеры.ЛюбоеЗначение(),
        Матчеры.ЛюбоеЗначение()
    ).ТогдаВозвращает(МокЛоггер);
    Сдк = Новый ОтелSdk(Неопределено, МокПров);

    // Когда
    Результат = Сдк.ПолучитьЛоггер("mylib", "2.0");

    // Тогда
    Ожидаем.Что(Результат).Равно(МокЛоггер);
    МокПров.ПроверитьЧтоВызывалсяМетод().ПолучитьЛоггер("mylib", "2.0");

КонецПроцедуры

&Тест
Процедура ПостроительЛоггераShortcut() Экспорт

    // Дано
    МокПров = Мок.Получить(Тип("ОтелПровайдерЛогирования"));
    МокПостроитель = Новый Структура("Имя", "построитель");
    МокПров.Когда().ПостроительЛоггера(
        Матчеры.ЛюбоеЗначение()
    ).ТогдаВозвращает(МокПостроитель);
    Сдк = Новый ОтелSdk(Неопределено, МокПров);

    // Когда
    Результат = Сдк.ПостроительЛоггера("mylib");

    // Тогда
    Ожидаем.Что(Результат).Равно(МокПостроитель);
    МокПров.ПроверитьЧтоВызывалсяМетод().ПостроительЛоггера("mylib");

КонецПроцедуры

&Тест
Процедура ПолучитьМетрShortcut() Экспорт

    // Дано
    МокПров = Мок.Получить(Тип("ОтелПровайдерМетрик"));
    МокМетр = Новый Структура("Имя", "метр");
    МокПров.Когда().ПолучитьМетр(
        Матчеры.ЛюбоеЗначение(),
        Матчеры.ЛюбоеЗначение()
    ).ТогдаВозвращает(МокМетр);
    Сдк = Новый ОтелSdk(Неопределено, Неопределено, МокПров);

    // Когда
    Результат = Сдк.ПолучитьМетр("mylib", "3.0");

    // Тогда
    Ожидаем.Что(Результат).Равно(МокМетр);
    МокПров.ПроверитьЧтоВызывалсяМетод().ПолучитьМетр("mylib", "3.0");

КонецПроцедуры

&Тест
Процедура ПостроительМетраShortcut() Экспорт

    // Дано
    МокПров = Мок.Получить(Тип("ОтелПровайдерМетрик"));
    МокПостроитель = Новый Структура("Имя", "построитель");
    МокПров.Когда().ПостроительМетра(
        Матчеры.ЛюбоеЗначение()
    ).ТогдаВозвращает(МокПостроитель);
    Сдк = Новый ОтелSdk(Неопределено, Неопределено, МокПров);

    // Когда
    Результат = Сдк.ПостроительМетра("mylib");

    // Тогда
    Ожидаем.Что(Результат).Равно(МокПостроитель);
    МокПров.ПроверитьЧтоВызывалсяМетод().ПостроительМетра("mylib");

КонецПроцедуры
