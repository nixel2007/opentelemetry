// Класс трассировщика (Tracer) по спецификации OpenTelemetry.
//
// Создает спаны для отслеживания операций.
// Получается из ОтелПровайдерТрассировки.

Перем мИмяБиблиотеки;
Перем мВерсияБиблиотеки;
Перем мРесурс;
Перем мПроцессор;

// Начинает новый корневой спан.
//
// Параметры:
//   ИмяСпана - Строка - имя спана
//
// Возвращаемое значение:
//   ОтелСпан
//
Функция НачатьСпан(ИмяСпана) Экспорт
    Возврат Новый ОтелСпан(ИмяСпана, мРесурс, мИмяБиблиотеки, мВерсияБиблиотеки, мПроцессор);
КонецФункции

// Начинает новый дочерний спан.
//
// Параметры:
//   ИмяСпана - Строка - имя спана
//   РодительскийСпан - ОтелСпан - родительский спан
//
// Возвращаемое значение:
//   ОтелСпан
//
Функция НачатьДочернийСпан(ИмяСпана, РодительскийСпан) Экспорт
    ИдТрассировки = РодительскийСпан.КонтекстСпана().ИдТрассировки();
    ИдРодителя = РодительскийСпан.КонтекстСпана().ИдСпана();
    Возврат Новый ОтелСпан(ИмяСпана, мРесурс, мИмяБиблиотеки, мВерсияБиблиотеки, мПроцессор, ИдТрассировки, ИдРодителя);
КонецФункции

// Конструктор трассировщика.
//
// Параметры:
//   ИмяБиблиотеки - Строка - имя инструментирующей библиотеки
//   ВерсияБиблиотеки - Строка - версия инструментирующей библиотеки
//   Ресурс - ОтелРесурс - ресурс
//   Процессор - ОтелПростойПроцессорСпанов - процессор спанов (необязательный)
//
Процедура ПриСозданииОбъекта(ИмяБиблиотеки, ВерсияБиблиотеки, Ресурс, Процессор = Неопределено)
    мИмяБиблиотеки = ИмяБиблиотеки;
    мВерсияБиблиотеки = ВерсияБиблиотеки;
    мРесурс = Ресурс;
    мПроцессор = Процессор;
КонецПроцедуры
