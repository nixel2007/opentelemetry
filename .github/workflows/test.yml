name: Тестирование

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    uses: autumn-library/workflows/.github/workflows/test.yml@v1
    with:
      test_engine: oneunit
      os_versions: '["ubuntu-latest"]'
      test_recursive: true
      additional_pip_packages: protobuf

